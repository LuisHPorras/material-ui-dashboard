import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#ff4054',
      secundary: '#ff8591'
    },
    secondary: {
      main: '#ffffff',
    },
  },
});

export default theme;